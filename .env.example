# Local Docker Compose Postgres (see docker-compose.yml):
DATABASE_URL=postgresql://pathwise:pathwise@localhost:5432/pathwise

# Or a hosted instance, e.g. Neon:
# DATABASE_URL=postgresql://user:password@ep-example-12345.region.aws.neon.tech/pathwise?sslmode=require

SECRET_KEY=change-me-in-production

# Optional — only needed for /assistant
# OPENROUTER_API_KEY=sk-or-v1-...
# OPENROUTER_MODEL=openai/gpt-4o-mini
